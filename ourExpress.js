var httpObj = require("http");

var app = {
  var port = process.env.PORT || 4000;
  var host = "localhost";
  var StartServer = function(path,callback) {
//create server
let server = httpObj.createServer(callback);
server.listen(port,host, fuction(req,res){});


//create listener

    return
  };
  var Get = function() {
    return
  };
};

var GetApp = function() {
  return app;
};
